# ==============================================================================
# Debate-AI (AI Live Fact Checker) — Full Requirements
# ==============================================================================

# ── Data acquisition & cleaning ──
datasets
huggingface_hub
requests
trafilatura
beautifulsoup4
lxml
ftfy
langdetect
datasketch
pandas
pyarrow
playwright==1.61.0
greenlet==3.5.3
pyee==13.0.1
# run once after install: playwright install chromium

# ── Audio ──
faster-whisper
pyannote.audio
webrtcvad-wheels
ffmpeg-python
soundfile
librosa
pyloudnorm

# ── NLP / embeddings / RAG ──
spacy
sentence-transformers
faiss-cpu

# ── LLM ──
openai
tiktoken                 # token counting for chunking + prompt budget checks

# ── Agents / orchestration ──
pydantic>=2.0
tenacity                  # retry logic for flaky LLM/API calls

# ── Backend ──
fastapi
uvicorn[standard]
redis
sqlalchemy
alembic
psycopg2-binary
python-dotenv
websockets
httpx

# ── Testing ──
pytest
pytest-asyncio
pytest-mock
pytest-cov                # coverage reporting, useful before demo day

# ── Torch (install separately per CPU/GPU) ──
# torch, torchaudio — see walkthrough's CPU install command
